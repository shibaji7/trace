[build-system]
requires = ["setuptools>=61.0", "setuptools-scm[toml]>=8", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hfpytrace"
dynamic = ["version"]
description = "Precision ionospheric radio wave propagation tools"
readme = "README.md"
requires-python = ">=3.11"
authors = [{name = "Shibaji Chakraborty"}]
license = {text = "MIT"}

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "--cov=trace --cov-report=xml --cov-report=term-missing"
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["trace"]

[tool.coverage.report]
show_missing = true
skip_covered = true

[tool.coverage.xml]
output = "coverage.xml"

[project.optional-dependencies]
dev = [
  "pytest",
  "pytest-cov",
  "coverage",
  "numpy",
  "pandas",
  "matplotlib",
  "pytesseract",
  "loguru",
  "requests",
  "beautifulsoup4",
  "tqdm",
  "lxml",
  "toml",
  "pytz",
  "SciencePlots",
  "scipy",
]
