[build-system]
requires = ["setuptools>=61.0", "setuptools-scm[toml]>=8", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hfpytrace"
dynamic = ["version"]
description = "Precision ionospheric radio wave propagation tools"
readme = "README.md"
requires-python = ">=3.11"
authors = [{name = "Shibaji Chakraborty"}]
license = {text = "MIT"}

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "--cov=hfpytrace --cov-branch --cov-report=term-missing:skip-covered --cov-report=xml --cov-report=html --cov-report=json"
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["hfpytrace"]

[tool.coverage.report]
show_missing = true
skip_covered = true

[tool.coverage.xml]
output = "coverage.xml"

[tool.coverage.html]
directory = "htmlcov"

[tool.coverage.json]
output = "coverage.json"

[tool.setuptools]
include-package-data = true

[tool.setuptools.package-data]
hfpytrace = ["cfg/*.json"]

[project.optional-dependencies]
dev = [
  "pytest",
  "pytest-cov",
  "coverage",
  "numpy",
  "pandas",
  "matplotlib",
  "pytesseract",
  "loguru",
  "requests",
  "beautifulsoup4",
  "tqdm",
  "lxml",
  "toml",
  "pytz",
  "SciencePlots",
  "scipy",
  "PyIRI",
]
